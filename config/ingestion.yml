cfpb:
  dataset_name: cfpb_complaints
  output_format: parquet
  timeout_seconds: 60
  mode: full_history

fred:
  dataset_name: fred_series
  output_format: parquet
  timeout_seconds: 60
  file_type: json
  observation_start: "2000-01-01"
